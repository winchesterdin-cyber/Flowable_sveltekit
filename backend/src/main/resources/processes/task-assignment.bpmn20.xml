<?xml version="1.0" encoding="UTF-8"?>
<definitions xmlns="http://www.omg.org/spec/BPMN/20100524/MODEL"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xmlns:flowable="http://flowable.org/bpmn"
             targetNamespace="http://www.flowable.org/processdef">

  <process id="task-assignment" name="Task Assignment" isExecutable="true">
    <startEvent id="startEvent" />
    <sequenceFlow sourceRef="startEvent" targetRef="taskWork" />

    <userTask id="taskWork" name="${title}" flowable:assignee="${assignee}" flowable:candidateGroups="USER">
        <documentation>${description}</documentation>
    </userTask>
    <sequenceFlow sourceRef="taskWork" targetRef="endEvent" />

    <endEvent id="endEvent" />
  </process>
</definitions>
