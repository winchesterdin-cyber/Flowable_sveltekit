# =========================================
# Flowable + SvelteKit BPM Demo
# Environment Variables Configuration
# =========================================

# -----------------------------------------
# Frontend Configuration
# -----------------------------------------

# Backend API URL for client-side requests
# Leave empty in production (nginx handles routing)
VITE_API_URL=

# Backend URL for server-side proxy requests
BACKEND_URL=http://localhost:8080

# Origin for CORS validation
ORIGIN=http://localhost:3000

# -----------------------------------------
# Backend Configuration (Spring Boot)
# -----------------------------------------

# Server port (default: 8080)
SERVER_PORT=8080

# Database configuration (H2 in-memory by default)
# For production, configure external database:
# SPRING_DATASOURCE_URL=jdbc:postgresql://host:5432/flowable
# SPRING_DATASOURCE_USERNAME=flowable
# SPRING_DATASOURCE_PASSWORD=secret

# Logging level
LOGGING_LEVEL_ROOT=INFO
LOGGING_LEVEL_ORG_FLOWABLE=DEBUG

# -----------------------------------------
# Docker Compose Configuration
# -----------------------------------------

# Node environment
NODE_ENV=production

# -----------------------------------------
# Railway/Cloud Deployment
# -----------------------------------------

# Railway provides PORT automatically
# PORT=8080

# Health check endpoint
# HEALTH_ENDPOINT=/health
