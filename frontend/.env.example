# Backend API URL
# In development, leave empty or use http://localhost:8080
# In production with Docker, the nginx proxy handles routing
VITE_API_URL=

# Backend URL for server-side requests (hooks.server.ts)
# Used when SvelteKit needs to proxy API requests
BACKEND_URL=http://localhost:8080

# Origin for CORS (required for production)
# Example: https://your-app.netlify.app
ORIGIN=http://localhost:3000
